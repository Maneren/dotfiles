#!/bin/zsh

function powerline_precmd() {
    local ARGS=""
    
    ARGS+=" -shell zsh"
    ARGS+=" -error $?"
    ARGS+=" -modules time,venv,user,ssh,cwd,perms,git,exit,root"
    ARGS+=" -max-width 70"
    ARGS+=" -hostname-only-if-ssh"
    
    PS1=$(powerline-go $(echo $ARGS))
}
